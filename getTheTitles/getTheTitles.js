// function getTheTitles(obj) {

//     let allBooks = obj.map(book => book.title);
//     return allBooks;

// }

// or

function getTheTitles(obj) {

    return obj.map(book => book.title);

}

module.exports = getTheTitles;
